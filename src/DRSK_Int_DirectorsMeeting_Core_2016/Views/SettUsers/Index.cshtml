@model IEnumerable<DRSK_Int_DirectorsMeeting_Core_2016.Domain.Entities.SettUser>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValInt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValStr)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValInt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValStr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { UserId = item.UserId, SettingId = item.SettingId }) |
            @Html.ActionLink("Details", "Details", new { UserId = item.UserId, SettingId = item.SettingId }) |
            @Html.ActionLink("Delete", "Delete", new { UserId = item.UserId, SettingId = item.SettingId })
        </td>
    </tr>
}
</table>
